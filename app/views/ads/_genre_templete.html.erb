	<table class="table" id = none-border>
		<thead>
			<th>ジャンル検索</th>
		</thead>
		<% genres.each do |genre| %>
			<tr>
				<td><%= link_to genre.genre_name, genre_search_ad_path(genre.id), :style=>"color:black;" %></td>
			</tr>
		<% end %>
	</table>