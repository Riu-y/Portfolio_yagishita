<div class="col-md-3">
	<table class="table" id = none-border>
		<thead>
			<th>ジャンル検索</th>
		</thead>
		<% genres.each do |genre| %>
			<tr>
				<td><%= link_to genre.genre_name, :style=>"color:black;" %></td>
			</tr>
		<% end %>
	</table>
</div>