<table class="sidebar">
	<thead>
		<th class="side-menu-heading">ジャンル検索</th>
	</thead>
	<% genres.each do |genre| %>
		<tr>
			<td><%= link_to genre.genre_name, genre_search_ad_path(genre.id), class:"side-menu" %></td>
		</tr>
	<% end %>
</table>
