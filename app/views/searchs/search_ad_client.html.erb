<div class="row">
	<div class="col-md-3">
		<!-- プロフィール -->
		<% if ad_client_signed_in? %>
			<%= render 'ad_clients/sidebar', ad_client: current_ad_client %>
		<% elsif driver_signed_in?%>
			<%= render "drivers/sidebar", driver: current_driver %>
		<% end %>
	</div>
	<div class="col-xs-8">
		<% if @model == 'driver' %>
			<%= render 'drivers/index', drivers: @records %>
		<% elsif @model == 'address' %>
			<%= render 'drivers/index', drivers: @records %>
		<% end %>
	</div>
</div>